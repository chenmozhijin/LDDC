name: lddc_lyrics_runtime
description: LDDC 的无 UI 歌词搜索、获取、翻译、匹配和持久化运行时。
version: 0.1.0-dev.1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  archive: ^4.0.9
  brotli: ^0.6.0
  crypto: ^3.0.7
  dart_taglib: ^0.1.0
  drift: ^2.34.3
  lddc_lyrics_core: ^0.1.0-dev.1
  path: ^1.9.1
  sqlite3: ^3.5.0
  web: ^1.1.1

dev_dependencies:
  lints: ^6.1.0
  pointycastle: ^4.0.0
  test: ^1.31.1
