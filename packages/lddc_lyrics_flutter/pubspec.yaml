name: lddc_lyrics_flutter
description: LDDC 的通用 Flutter 歌词组件与平台适配。
version: 0.1.0-dev.1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.0
  flutter: ">=3.47.0-0.2.pre"

dependencies:
  file_selector: ^1.1.0
  flutter:
    sdk: flutter
  lddc_lyrics_core: ^0.1.0-dev.1
  lddc_lyrics_runtime: ^0.1.0-dev.1
  path: ^1.9.1
  path_provider: ^2.1.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  test: ^1.31.1

flutter:
  assets:
    - assets/ne_device_ids.txt.gz
